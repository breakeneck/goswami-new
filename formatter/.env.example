# Database settings
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Media files root prefix (path to audio files)
MEDIA_ROOT_PREFIX=

# LM Studio API settings
LM_STUDIO_API_URL=
LM_STUDIO_MODEL=

# Processing settings
REQUEST_TIMEOUT=300
MAX_RETRIES=3
TEMPERATURE=0.3
# Dynamic timeout: seconds per minute of audio (e.g., 5 = 5min timeout per 1min of audio)
TIMEOUT_PER_MINUTE=5
